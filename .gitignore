# Environments & data
.venv/
venv/
/data/

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Tooling caches
.ruff_cache/
.pytest_cache/
*.egg-info/

# Training artifacts — regenerated by anyone who reruns the fits, so they stay out of git
checkpoints/
outputs/
catboost_info/
lightning_logs/
logs/
*.pt
*.ckpt

# ...except the graded artifact itself, which DOES ship here. It is 55 MB and it is the model
# every number in this repository describes; a clone without it records the model nowhere.
# Verify it with scripts/verify_checkpoint.sh.
!checkpoint.pt

# Generated prediction CSVs — reproducible from predict.py + the checkpoint
predictions.csv
predictions_*.csv

# Submission archive: built by scripts/make_submission.py, never committed
submission/

# OS
.DS_Store

# Local env
.env
