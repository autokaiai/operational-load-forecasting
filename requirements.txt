# TRAINING + EVALUATION dependencies. For inference only, use requirements-inference.txt —
# it is narrower and it is the set the graded archive actually shipped.
#
# Dev tooling (ruff, pytest, pre-commit) and the optional EDA libraries live in pyproject.toml:
#   pip install -e ".[dev]"    pip install -e ".[eda]"
#
# On a CPU-only machine install torch first, or pip will pull ~3 GB of CUDA wheels:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
pandas>=2.2
numpy>=1.26
pyyaml>=6

# Forecasting stack. PINNED, not ranged: every recorded number in results/ was produced against
# these versions, and 3.1.9 and 3.2.0 differ behaviourally (3.1.9's get_activation_fn("GELU")
# returns F.elu; 3.2.0 returns F.gelu). neuralforecast transitively pulls coreforecast, ray, optuna.
neuralforecast==3.1.9
pytorch-lightning==2.5.6
utilsforecast==0.2.16

# Gradient-boosted members and stackers (src.models.lgbm, src.models.catboost_tree). CPU-only.
lightgbm>=4.0
catboost>=1.2
scikit-learn>=1.3
xgboost>=2.1

# The two Chronos-2 members. Needed to reproduce the cascade covariate and the fine-tune, and to
# run inference at rung 1.
chronos-forecasting>=2.0
huggingface_hub>=0.24
